#include "holberton.h"
#include <string>
/**
*_strcat - concatinates two strings and store it in dest
*@dest: the destination
*@src: the source
*Return:the dest value
*/
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
